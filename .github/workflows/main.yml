name: Python application

on:
  push:
   branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
      
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build & Push Image
      run: |
        echo "${{ secrets.DEVOPS }}" | docker login -u Praggya Singh --password-devops123
        docker image build -t Praggya Singh/pyflask:latest .
        docker push d Praggya Singh/pyflask:latest
